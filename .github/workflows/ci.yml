name: ci

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, edited]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  required:
    name: ci / required
    runs-on: ubuntu-latest
    steps:
      - name: Temporary post-fix self-modification audit
        run: echo 'temporary audit check'
