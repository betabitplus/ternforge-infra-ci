# ternforge-infra-ci

Shared CI and release building blocks for Ternforge repositories.

The initial repository lifecycle is established by Ternforge Task 0001. During
that task this repository intentionally contains a local role-specific required
workflow. Reusable CI and release workflows are introduced in Task 0002 after
the protected main-only lifecycle has been proven end to end.
